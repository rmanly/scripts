#!/bin/bash


while read -r -d $'\0'; do
    new_name=$(echo "$REPLY" | tr ":" "-")
    mv "$REPLY" "$new_name"
done < <(find . -iname "*:*" -print0)


# e36freak came up with this which is soo awesome

find . -type f -name '*:*' -exec bash -c 'for arg; do mv "$arg" "${arg//:/-}"; done' _ {} +
